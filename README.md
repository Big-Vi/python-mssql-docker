# Dockerize Python MSSQL app

## Local setup

Run `docker compose up`.


### Lint Dockerfile for best practices:

https://github.com/hadolint/hadolint
`docker run --rm -i ghcr.io/hadolint/hadolint < Dockerfile.local`